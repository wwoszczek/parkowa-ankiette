# Direct dependencies (plain pip). Streamlit Community Cloud installs from this.
streamlit[auth]>=1.47.0   # [auth] extra = Authlib, needed by st.login
httpx>=0.27               # required by Authlib's starlette OAuth client
psycopg2-binary>=2.9.7
python-dotenv>=1.0.0
pytz>=2023.3
PyYAML>=6.0.0
